plugins {
    alias(libs.plugins.android.application)
}

android {
    namespace = "com.gncbrown.getmetosleep" // From your Java package
    compileSdk = 34 // A recent stable SDK version

    defaultConfig {
        applicationId = "com.gncbrown.getmetosleep"
        minSdk = 23 // A common minimum SDK
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"

        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled = false
            proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
        }
    }
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }
}

dependencies {
    implementation(libs.appcompat)
    implementation(libs.material)
    // You can add other dependencies from your libs.versions.toml here as needed
    // implementation(libs.gson)
    // implementation(libs.play.services.nearby) 

    testImplementation(libs.junit)
    androidTestImplementation(libs.ext.junit)
    androidTestImplementation(libs.espresso.core)
}
